spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql-standalone:3306/netflix_db?allowPublicKeyRetrieval=true&useSSL=false
    username: netflix_user
    password: netflix_user
    hikari:
      initialization-fail-timeout: 0